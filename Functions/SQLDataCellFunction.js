//const SQLDataCells = require('./SQLDataCells');

const fs = require('fs');

  // Import SQLRecordsCellArr
    //const SQLRecordsCellArr = require('./Functions/SQLDataCol.json');
/* 
    const test = {
        activity: [...new Set(activityArr)],
        state: [...new Set(stateArr)],
        concerned: [...new Set(concernedArr)],
        type: [...new Set(typeArr)],
        place: [...new Set(placeArr)],
        content: [...new Set(contentArr)]
    }
    
    fs.writeFile('./Functions/SQLDataCol.json', JSON.stringify(test, null, 1), function(err) {
        if(err) {
            return console.log(err);
        }
        console.log("The file was saved!");
    }); */